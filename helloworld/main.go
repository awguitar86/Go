package main

import "fmt"

func main() {
	fmt.Println("Hi there!")
}

/*
go build => Compiles a bunch of go course code files.

go run => Compiles and executes one or two files.

go fmt => Formats all the code in each file in the current directory.

go install => Compiles and "installs" a pachage.

go get => Downloads the raw source code of someone else's package.

go test => Runs any tests associated with the current project.
*/

/*
package main => Defines a pacakage that can be compiled and then executed. Must have a func called 'main'.
*/

/*
fmt => Short for format.
*/

/*
func => Short for function. Works like a function in JavaScript or Ruby.
*/
